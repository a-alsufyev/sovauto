version: "3.8"

services:
  sovauto:
    build: .
    container_name: sovauto
    restart: always

    ports:
      - "5000:5000"

    env_file:
      - .env

    environment:
      - NODE_ENV=production